{% include 'default/_form.html.twig' with {
    page_title: 'title.task_delete'|trans({'%id%': task.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'task_index',
} %}